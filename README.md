money-garden
============

Private data
------------
Use `config/budgets.private.json` and `data/sample_transactions.private.csv` for
your real data. These files are gitignored. The committed samples live in
`config/budgets.json` and `data/sample_transactions.csv`.
